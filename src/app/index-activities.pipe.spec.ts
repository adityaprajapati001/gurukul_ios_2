import { IndexActivitiesPipe } from './index-activities.pipe';

describe('IndexActivitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new IndexActivitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
