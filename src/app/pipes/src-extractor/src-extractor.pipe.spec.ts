import { SrcExtractorPipe } from './src-extractor.pipe';

describe('SrcExtractorPipe', () => {
  it('create an instance', () => {
    const pipe = new SrcExtractorPipe();
    expect(pipe).toBeTruthy();
  });
});
