declare module 'pipwerks-scorm-api-wrapper' {
    // Define the types here based on the module's API.
    // For now, you can use `any` as a placeholder.
    const scorm: any;
    export default scorm;
}
